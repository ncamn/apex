import React from "react";
import CurrencyView from "./CurrencyView";
import { shallow } from "enzyme";

describe("<CurrencyView/>", () => {
  /*
    it('exists', () => {
        const wrapper = shallow(<CurrencyView/>);

        expect(wrapper.exists()).toEqual(true);
    });
    */
});
