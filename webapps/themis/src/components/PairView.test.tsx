import React from "react";
import PairView from "./PairView";
import { shallow } from "enzyme";

describe("<PairView/>", () => {
  /*
    it('exists', () => {
        const wrapper = shallow(<PairView/>);

        expect(wrapper.exists()).toEqual(true);
    });
    */
});
